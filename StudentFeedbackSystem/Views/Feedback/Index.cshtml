@{ ViewData["Title"] = "Feedback Overview"; }

<div class="card">
    <div class="card-header"><h5><i class="bi bi-clipboard-data me-2"></i>Feedback Overview</h5></div>
    <div class="card-body">
        <div class="text-center py-5">
            <i class="bi bi-clipboard-data" style="font-size:4rem; color:var(--primary);"></i>
            <h4 class="mt-3 fw-bold">Feedback Management</h4>
            <p class="text-muted">Use the sidebar to manage feedback questions or view submitted feedback data.</p>
            <div class="d-flex justify-content-center gap-3 mt-3">
                <a href="/FeedbackQuestions" class="btn btn-primary"><i class="bi bi-question-circle me-1"></i> Manage Questions</a>
            </div>
        </div>
    </div>
</div>
